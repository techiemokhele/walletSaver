//export all the files in this directory
import icons from "./icons";
import images from "./images";
import theme, { COLORS, SIZES, FONTS } from "./theme";

export { icons, images, theme, COLORS, SIZES, FONTS };
